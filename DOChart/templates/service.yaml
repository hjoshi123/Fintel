apiVersion: v1
kind: Service
metadata:
  name: {{ include "DOChart.fullname" . }}
  labels:
    {{- include "DOChart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "DOChart.selectorLabels" . | nindent 4 }}
